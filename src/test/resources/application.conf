kafka {
  topics = "test"
  brokers = "localhost:9092,localhost:9093,localhost:9094"
}

jdbc {
  driver = "org.h2.Driver"
  url = "jdbc:h2:~/test:hello"
  user = "user"
  password = "pass"
}

checkpointDir = "checkpointdir"

streaming {
  batch = 2
}